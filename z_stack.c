#include "monty.h"

/**
 * z_stack - Prints the top element of the stack
 * @top: Pointer to the stack's head
 * @counter: Line number
 * Return: No return value
 */
void z_stack(stack_t **top, unsigned int counter)
{
	(void)top;
	(void)counter;
	bt.lifi = 0;
}
