#include "monty.h"

/**
 * z_nop - Represents a function with no operation.
 * @top: Points to the stack head.
 * @counter: Represents the line number.
 * Return: This function has no return value.
 */
void z_nop(stack_t **top, unsigned int counter)
{
	(void) counter;
	(void) top;
}
